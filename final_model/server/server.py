import socket
import time

# IP = "localhost"
# PORT = 5000
#
# sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
# sock.bind((IP, PORT))
#
# while True:
#     data, addr = sock.recvfrom(1024)
#     print(data.decode())
#     time.sleep(1)


